<?xml version="1.0" encoding="utf-8"?>
<!--
  ابزارک صفحه خانه.

  RemoteViews است و نه Glance: افزودن Glance یک وابستگی تازه می‌خواهد و
  این ابزارک آن‌قدر ساده است که ارزشش را ندارد.

  نوار پیشرفت حتماً ProgressBar است و نه دو View روی هم: RemoteViews
  نمی‌تواند عرضِ یک View را در زمان اجرا عوض کند، اما setProgressBar را
  مستقیم پشتیبانی می‌کند.

  چیدمان راست‌به‌چپ است تا با بقیه اپ بخواند.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layoutDirection="rtl"
    android:gravity="center_vertical"
    android:padding="12dp"
    android:background="@drawable/widget_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- شعله برداری، نه اموجی: اموجی روی هر گوشی شکل دیگری دارد -->
        <ImageView
            android:id="@+id/widget_flame"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:contentDescription="@string/widget_description"
            android:src="@drawable/ic_flame_dim" />

        <TextView
            android:id="@+id/widget_streak"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:textSize="24sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:text="۰ روز"
            android:textColor="#FF8C00" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="12sp"
        android:maxLines="2"
        android:text="برای شروع بزن"
        android:textColor="#5A5A5A" />

    <ProgressBar
        android:id="@+id/widget_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:layout_marginTop="8dp"
        android:max="100"
        android:progressDrawable="@drawable/widget_progress" />

    <TextView
        android:id="@+id/widget_goal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:textSize="11sp"
        android:maxLines="1"
        android:text="SpeakUp"
        android:textColor="#8A8A8A" />
</LinearLayout>
