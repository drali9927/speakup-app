/**
 * دسته‌بندی انواع فعالیت — ترجمه data/model/Enums.kt.
 *
 * دو تصمیم که در اپ مستند شده و این‌جا هم باید یکی بماند:
 *
 * **تولیدی در برابر تشخیصی** — متریک داوری محصول (سند ۰۷ بخش ۷.۷) فقط
 * تمرین‌های تولیدی را می‌شمارد. اگر وب چندگزینه‌ای را تولیدی حساب کند،
 * عددی که در پروفایل دیده می‌شود با عددی که اپ نشان می‌دهد نمی‌خواند.
 *
 * **نمره‌دار در برابر نمایشی** — کارت آموزشی، کارت واژه، گفت‌وگو و
 * داستانک سوالی نمی‌پرسند، پس «۰ از ۱ درست» برایشان بی‌معنا و
 * دلسردکننده است.
 */

/** تمرین‌هایی که کاربر باید خودش زبان تولید کند */
export const PRODUCTIVE = new Set([
  'FILL_BLANK',
  'FREE_TEXT',
  'REORDER',
  'TRANSLATE_TO_EN',
  'SPEAKING',
  'LISTENING',
])

/** فعالیت‌هایی که پاسخ دارند و نمره‌شان معنا دارد */
export const SCORED = new Set([
  ...PRODUCTIVE,
  'MULTIPLE_CHOICE',
  'MATCHING',
  'WORD_MATCH',
])
